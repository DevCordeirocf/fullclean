# Fullclean - Multi-tenant Project
Este projeto implementa as etapas de segurança e isolamento de dados multi-tenant para a aplicação Fullclean, incluindo o `TenantIdFilter` e a configuração de isolamento de dados via Hibernate.
